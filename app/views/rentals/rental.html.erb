<style>
  .button {
    background-color: green;
    color: black;
    padding: 50px 10px;
    text-align: center;
    display: inline-block;
    font-size: 16px;
    border-radius: 5px;
  }
</style>

<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">
    <div class="section-title">

      <h1>Choose Your Bike</h1>
      <p>You are at <%= @station.name %>.</p>
      
        <% @station.docks.each do |dock| %>
            <% if dock.bike.identifier %>
              <button class="button"> <%= link_to(dock.bike.identifier, "/purchase_confirmation") %> </button>
            <% else %>
              <button class="button"> <%= dock.bike.identifier %> </button>
            <% end %>
        <% end %>

      <p> There are <%= @station.docks.count %> docks here.
      <p> There are <%= @station.bikes.count %> bikes here.

      
    </div>  
  </div>
</div>